export type DateString = `${number}-${number}-${number}`;
// type personalisé pour représenter des dates au format AAAA-MM-JJ


export type user_taskType = {
    task_id: number;
    user_id: number;
}
